- name: installing mysql-client
  yum:
    name: "{{mysql_client}}"
    state: installed

- name: load the data
  command: mysql -h {{hostvars['mysql'].ansible_host}} -uroot -p{{mysql_client_password}} < {{app_path}}/schema/{{component}}.sql
